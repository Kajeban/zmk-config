# 🛑 Disable USB Logging (Not supported on XIAO BLE)
CONFIG_ZMK_USB_LOGGING=n 

# ✅ Enable BLE Logging Instead
CONFIG_ZMK_SPLIT_BLE_LOGGING=y  
CONFIG_LOG=y
CONFIG_LOG_DEFAULT_LEVEL=4

# ✅ Enable Serial Support (for debugging)
CONFIG_SERIAL=y
CONFIG_UART_CONSOLE=y
CONFIG_USB_DEVICE_STACK=n  # XIAO BLE does not support USB serial
CONFIG_USB_CDC_ACM=n

# ✅ Bluetooth Settings
CONFIG_BT_GATT_ENFORCE_SUBSCRIPTION=n
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
